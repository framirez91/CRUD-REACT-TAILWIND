const Error = ({ children }) => {
  return (
    <div className="tex-center my-4 bg-red-600 text-white font-bold p-3 uppercase text-center">
      {children}
    </div>
  );
};

export default Error;
